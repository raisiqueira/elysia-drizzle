# Elysia with Bun runtime + Drizzle ORM

## Running the project

Start the Docker containers:

```bash
docker-compose up -d
```

Install dependencies:

```bash
bun install
```

Run the migrations:

```bash
bun migrate
```

Start the server:

```bash
bun dev
```